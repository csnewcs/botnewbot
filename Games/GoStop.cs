using System;

namespace csnewcs.Game.GoStop
{
    class GoStop
    {
        const int hwatuCardCount = 50;
        private readonly int playerCount;
        public GoStop(ulong[] ids)
        {
            
        }
    }
}